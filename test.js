//to start the backend server if you have a data json file if you have the file data.json then run this
//  command and the server should we started

//  ***commands ***
// Set-ExecutionPolicy -Scope process -ExecutionPolicy Bypass
//json-server --watch data.json --port 8080
